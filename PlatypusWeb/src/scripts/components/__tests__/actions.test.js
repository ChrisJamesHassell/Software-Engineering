import * as axtons from '../../actions/actions';
import { task } from '../dataFixtures/fixtures';

describe ('actions', () => {
    it('creates action', () =>{
        const expectedAction = { type: axtons.SET_PIN_FILTER, task };

        // expect(actions.)

    });
});


